# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.
shared:
  gmail_account: <%= ENV["GMAIL_ACCOUNT"] %>
  gmail_account_password: <%= ENV["GMAIL_PASSWORD"] %>

development:
  secret_key_base: b6a7802cc929c693e6b4f835fc1f5352cc4a3479915a49cf06e26fa274f13b308e6b0d5bd55f4acb367e99712c36e49fb9df6a7a26a9c6ec4ec48a04b77068e9

test:
  secret_key_base: 9c66259cf15e74412af7f6cbdcf4e0520a6347a1ba20cc36f184997a7f4337efe6d564df411ff01712b602217e3714120503b2a7aedc95b41825add0f467886e

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
