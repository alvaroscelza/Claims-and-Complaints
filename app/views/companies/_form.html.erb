<%= form_with(model: company, local: true) do |form| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :company_name %>
  </div>

  <div class="field">
    <%= form.label :reputation %>
    <%= form.number_field :reputation, id: :company_reputation %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image, id: :company_image %>
  </div>

  <div class="field">
    <%= form.label :business %>
    <%= form.text_field :business, id: :company_business %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :company_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
